name: Verify RSS feeds 📡
on:
  schedule:
    - cron: 0 0 1 * *
  workflow_dispatch: ~
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Verify RSS feeds
        run: |   
            ./.github/workflows/verify-feeds.sh feeds.opml